# CUHKSZ Eats

CUHKSZ Eats 是由个人维护的校园餐饮信息静态网站。内容来自维护者本地的 `images/` 图片目录；源图片不会提交到 Git。

CUHKSZ Eats 是由个人独立维护的非官方网站，与香港中文大学（深圳）及站内所列商户无隶属、授权或认可关系。内容仅记录特定时间的个人用餐与实拍信息，不代表校方或商户的实时菜单、价格及承诺，请以现场和官方信息为准。信息纠错或图片移除请联系 `l0123i456@163.com`。

## 环境要求

- Python 3.12+
- [uv](https://docs.astral.sh/uv/)
- Node.js 20+

从全新检出安装锁定的 Python 与前端依赖：

```bash
uv sync --frozen
npm install
```

## 本地维护

检查默认的 `images/` 源目录，不改名、移动或写入任何源图片：

```bash
uv run python manage.py check
```

检查结果分为两类：

- `错误` 会阻止预览和发布门禁；包括非法目录层级、不支持或损坏的图片、同一归属内的源前缀重名，以及无效的结构化文件名或菜品价格。
- `警告` 会给出修正示例但允许继续；包括无法解析拍摄时间、疑似旧命名、疑似结构化内容和名称首尾空格。

每条诊断都会指出具体路径、原因和手工修正示例。命令只报告问题，不会交互询问，也不会自动移动、重命名或修改源文件。图片扩展名不区分大小写，支持 JPEG、PNG、WebP、HEIC 和 HEIF；`.DS_Store`、`Thumbs.db`、`desktop.ini` 与 AppleDouble 文件会被忽略。

通过同一检查后生成一次性本地内容，并启动 Vue/Vite 预览：

```bash
uv run python manage.py dev
```

默认地址是 `http://127.0.0.1:5173`。一次性产物写入被 Git 忽略的 `.generated/`，可随时删除重建。

图片派生使用 `.generated/cache/images/` 中的增量缓存。源图片内容和处理配置未变化时直接复用已有 WebP；新增或修改图片时只生成对应派生，已删除或重命名图片留下的公开文件会从预览目录清理。删除 `.generated/cache/` 可以强制重新生成全部派生图。

预览不会复制或修改源图片。每张图片都会从解码后的像素重新生成匿名的响应式 WebP：普通照片提供最高 1200 像素宽的多档尺寸，菜单照片提供最高 2400 像素宽的多档尺寸。浏览器会按显示区域选择合适版本，横竖构图均完整显示；照片分组和菜品历史中的照片均可点击放大，其中菜单使用高分辨率版本方便阅读。派生图不继承 GPS、设备型号或其他源元数据，只写入 UTF-8 XMP 权属字段；页面可见说明、图片结构化数据和文件元数据统一注明“CUHKSZ Eats 提供”，版权仍属于原摄影者。公开内容索引和生成目录只引用这些派生图，不提供原图入口。

预览会严格按目录归属展示内容：第一层目录是餐饮地点，第二层目录是档口；餐饮地点内散图不会被猜测归入档口。精确命名为 `菜单` 和 `门面` 的照片单独成组。地点级菜品散图始终展示；当地点同时含有档口时，散图上方显示“未归档”标题。根目录散图及 `_校园补充/` 下的相册只出现在首页次要的校园补充区域。

同一归属目录内名称完全相同的菜品记录会合并为一个菜品，地点与档口之间不会跨归属合并。菜品卡片使用最新的有日期记录作为封面，并摘要最近一次有日期的价格；菜品详情按时间从新到旧保留全部照片、日期、价格和“记录者随记”，无法解析时间的记录排列在最后。

首页搜索只匹配餐饮地点、档口和菜品名称；结果会标明类型及真实归属，并直接进入相应详情。记录者随记、普通照片、校园补充照片和校园补充相册不会进入搜索索引。地点、档口和菜品使用名称型稳定标识，并通过完整归属路径隔离同名内容。

生产构建会根据内容索引为每个详情网址生成独立的静态 `index.html`，并生成 `404.html` 回退，因此这些网址可在静态托管环境中直接打开和刷新：

```bash
npm run build
```

`publish` 目前已经接入与 `check`、`dev` 完全相同的前置检查；实际生成和更新 GitHub Pages 的发布阶段将在 Issue #10 实现。在此之前，校验通过后该命令会明确退出，不修改发布分支或远程站点：

```bash
uv run python manage.py publish
```

## 验收

安装 Playwright 的 Chromium 后运行完整的公开命令与浏览器验收：

```bash
npx playwright install chromium
npm run test:e2e
```

## 许可证与内容权利

仓库中的程序代码采用 [MIT License](LICENSE.md)。MIT 授权不适用于网站文字、`images/` 中的原始照片或构建生成的衍生图；这些内容的版权属于各自原权利人，未经许可不得复制、再发布或再利用。公开展示的照片由 CUHKSZ Eats 提供，版权仍属于原摄影者。
